export default class Clients {
     constructor(name, email, tags, assignedTo){
         this.name = name;
         this.email = email;
         this.tags = tags;
         this.assignedTo = assignedTo;
     }
}